import { APPLY_TAG_FILTER, CHANGE_TAB } from '../constants/action';

export default (state = {}, action) => {
    switch (action.type) {
        case APPLY_TAG_FILTER:
            return {
                
            }
    }
}